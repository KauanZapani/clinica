kind = "web"
previewPath = "/"
title = "Clínica de Cirurgia Plástica"
version = "1.0.0"
id = "artifacts/clinica"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 20881

[services.development]
run = "pnpm --filter @workspace/clinica run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/clinica", "run", "build" ]
publicDir = "artifacts/clinica/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "20881"
BASE_PATH = "/"
